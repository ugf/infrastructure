require 'rubygems'
require 'cucumber'
require 'cucumber/rake/task'

Cucumber::Rake::Task.new(:default) do |t|
  t.cucumber_opts = "#{node[:features_directory]} --format pretty --tags <%= @tags %>"
end
